# Core data processing
pandas==2.1.3
numpy==1.24.3
requests==2.31.0
python-dotenv==1.0.0

# Geospatial
geopandas==0.13.2
shapely==2.0.1

# Machine learning (baselines)
scikit-learn==1.3.2
xgboost==2.0.3
lightgbm==4.6.0   # bumped from 4.1.0: no py3.11/arm64 wheel for 4.1.0 and its sdist fails to build under CMake 4.x
imbalanced-learn==0.11.0

# Deep learning (LSTM / future ST-HAE re-implementation)
tensorflow==2.15.0
torch==2.1.0
# For the ST-HAE GCN re-implementation (see docs/ROADMAP.md), also install:
#   torch-geometric

# LLM integration (Phase 3: explainability)
openai>=2.0
anthropic>=0.40
transformers==4.35.0

# Visualization
matplotlib==3.8.1
seaborn==0.13.0
plotly==5.17.0
folium==0.14.0

# Notebooks / tooling
tabulate==0.9.0          # pandas .to_markdown() for robustness reports
jupyter==1.0.0
tqdm==4.66.1
pytest==7.4.3
nbconvert==7.9.2

# ---------------------------------------------------------------------------
# Optional GPU acceleration (NVIDIA CUDA 11 only — skip on CPU/macOS).
# Install separately if you have a CUDA environment:
#   pip install cudf-cu11 cuml-cu11 cugraph-cu11 dask-cudf-cu11 datashader holoviews
# ---------------------------------------------------------------------------
pyarrow==25.0.0
huggingface_hub>=0.24
scipy>=1.11
