# Lean, reproducible core — everything the forecasting + robustness + ST-HAE + faithfulness +
# figures pipeline and the test suite need. Installs fast and cleanly (no GDAL, no TensorFlow,
# no notebook stack). Use requirements.txt for the full environment (Keras-LSTM baseline,
# geospatial viz, notebooks). See Makefile / Dockerfile.
pandas==2.1.3
numpy==1.24.3
scipy>=1.11
scikit-learn==1.3.2
xgboost==2.0.3
torch==2.1.0
pyarrow==25.0.0
matplotlib==3.8.1
tabulate==0.9.0
requests==2.31.0
python-dotenv==1.0.0
# LLM providers for the faithfulness eval (Groq reuses the openai SDK)
openai>=2.0
anthropic>=0.40
huggingface_hub>=0.24
# tooling
pytest==7.4.3
